## Prototype Inheritance — Homework
📌 Description

This project demonstrates class inheritance in JavaScript using ES6 syntax.
The task was to create two classes where one inherits from another and overrides a method while still calling the parent method.
